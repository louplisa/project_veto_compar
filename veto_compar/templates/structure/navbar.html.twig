<nav class="navbar navbar-expand-lg bg-success">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">VetoCompar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('app_home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('app_contact') }}">Contactez-nous</a>
                </li>
                <li class="nav-item">
                    {% if not is_granted('ROLE_USER') or not is_granted('ROLE_ADMIN') %}
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    {% endif %}
                </li>
            </ul>
            <span class="navbar-text">
                {% if is_granted('ROLE_USER') or is_granted('ROLE_ADMIN') %}
                    Bienvenue {{ app.user.firstname }}
                {% endif %}
            </span>
        </div>
    </div>
</nav>