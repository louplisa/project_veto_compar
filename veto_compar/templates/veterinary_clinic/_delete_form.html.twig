<form method="post" action="{{ path('app_veterinary_clinic_delete', {'id': veterinary_clinic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ veterinary_clinic.id) }}">
    <button class="btn">Delete</button>
</form>
